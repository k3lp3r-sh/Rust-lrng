fn main() {
    {
        let s = "Hello, world!";
    } //after this scope is over, the memory of the funciton is given back to the system. the
    //memory used by the var s to hold the value is now nonexistent.
}
